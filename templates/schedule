// @enabled FALSE
// @mutex TRUE

// @on SCHEDULE "EVERY 30 SECOND"



import { Event, ScheduleEvent, log } from 'core';



export async function run (event : Event)
{
	if (!(event instanceof ScheduleEvent)) {
		return;
	}

	await log('scheduled endpoint tick');
}
