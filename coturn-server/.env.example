# Copy this file to .env and customize values for your host.

# Runtime UID/GID used inside the container. Match the owner of coturn-server/cert/
# on the host so certificate generation works without permission issues.
USER_UID=1000
USER_GID=1000